scan page for valid images
when valid images are hovered over, icon in corner
	add image to corner, set visibility for onhover
icon hover
	"kilect!"
	"more options"
		name
		directory to save to
		tags
		delete date
		secure save

kilections page
	directories sidebar
	default directory contents load
		drag and drop to directories
		list of name/url, option to show all thumbnails
		then load thumbnails of imgs
		when clicked takes to new tab of image